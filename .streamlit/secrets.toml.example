# Kopieer dit bestand naar `.streamlit/secrets.toml` en stel een wachtwoord in.
# Zonder dit bestand draait de app zonder authenticatie (handig voor lokale dev).
APP_PASSWORD = "vervang-dit-met-een-sterk-wachtwoord"
